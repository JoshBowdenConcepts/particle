import { color } from '../colors'
import { spacing } from '../spacing'

// This shouldn't go beyond two layers I don't think?
export const button = {
	background: color.primary['400'],
	padding: {
		vertical: spacing.spacing400,
		horizontal: spacing.spacing800
	},
	color: color.gray['100']
}